name = "bot2"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# --- Secrets (set via `wrangler secret put`, do NOT put real values here) ---
# TAVILY_API_KEY

[vars]
# no plaintext vars needed on this worker
